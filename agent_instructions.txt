1. Get rid of the list tab entirely. 
2. Rename the “sections” tab to be the new “list” tab
3. Add a “complete trip” button at the top of the shopping list and map page. Button should only appear when user checks off an item, thus starting a ‘shopping trip’. When the user hits “complete trip”, move all checked/crossed off items into a “shopping history” category. This master history list needs to be below all other categories, scrollable, and grouped by date so a user can open an old trip and see what they purchased. Trips should be the content of the list; clicking a trip should open the list of things purchased on that trip.
